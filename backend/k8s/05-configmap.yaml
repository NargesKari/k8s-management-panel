apiVersion: v1
kind: ConfigMap
metadata:
  name: kari-config
  namespace: kari-panel
data:
  POSTGRES_HOST: "postgres-service"
  POSTGRES_PORT: "5432"
  REDIS_URL: "redis://redis-service:6379/0"
  DJANGO_ALLOWED_HOSTS: "api.kari.osdl.ir"
  DJANGO_DEBUG: "0"
  BACKUP_ROOT: "/backups"
